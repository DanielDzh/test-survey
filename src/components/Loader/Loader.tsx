'use client';

import { LoadingContainer, Spinner } from './Loader.styled';

const Loader = () => {
  return (
    <LoadingContainer>
      <Spinner />
    </LoadingContainer>
  );
};

export default Loader;
